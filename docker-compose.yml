services:
  islasgeci:
    build:
      context: .
      dockerfile: Dockerfile
    image: islasgeci/diferencias_morfometria_posicion_trampas:latest
    command: bash
    volumes:
      - $PWD:/workdir
